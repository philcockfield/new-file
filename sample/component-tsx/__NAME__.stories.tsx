import { React, describe, knobs } from '../../common/storybook';
import { __NAME__ } from '.';


describe('__NAMESPACE__', {
  title: '__DESCRIPTION__',
})
  .add('__NAME__', () => {
    return (
      <__NAME__ />
    );
  });
